schema: https://tracert.site/schemas/manifest/v0.1
provider:
  id: tracert-selftest
  name: Tracert Self-Test
  url: https://tracert.site
service:
  id: tracert-selftest
capability:
  id: tracert-selftest.owned
  version: 0.0.1
  status: draft
  promise: Self-test capability verifying the domain-ownership gate accepts a
    valid, verified submission.
  input:
    schema_ref: ../schemas/owned.input.schema.json
    media_types:
      - application/json
  output:
    schema_ref: ../schemas/owned.output.schema.json
    media_types:
      - application/json
  interfaces:
    - type: native_api
      endpoint: https://tracert.site/selftest
  pricing:
    free: true
    mode: free
  operations:
    idempotency: supported
  data_policy:
    input_retention:
      policy: none
    training_use: none
  provenance:
    integration_status: first_party
